filebeat.inputs:
  - type: tcp
    max_message_size: 10MiB
    host: "0.0.0.0:4560"
processors:
  - add_fields:
      target: log_info
      fields:
        project_origin: dependency_status_scanner
output.logstash:
  hosts: [ "logstash:5044" ]
